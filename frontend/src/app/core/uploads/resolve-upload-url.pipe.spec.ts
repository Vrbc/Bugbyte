import { ResolveUploadUrlPipe } from './resolve-upload-url.pipe';

describe('ResolveUploadUrlPipe', () => {
  it('create an instance', () => {
    const pipe = new ResolveUploadUrlPipe();
    expect(pipe).toBeTruthy();
  });
});
